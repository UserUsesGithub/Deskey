A simple desmos keybinds library created for games.
To compile it, download the repository, and run the python script in the same directory that you're storing both of these in. It's going to ask for a javascript array, which when inputted will return the output of the compilation in a file and also in the console.
